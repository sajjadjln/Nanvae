import { Typography } from '@mui/material';

export default function CheckOutPage()
{
    return (
        <>
            <Typography variant='h3'>only logged users should see this</Typography>
        </>
    )
}